<h1>Users</h1>

<% User.all.each do |person| %>
  <%= person.first_name %>
   <%= link_to "Show", person %><br>
<% end %>

<!-- <% User.all.each do |person| %>
  <%= person.first_name %>
   <%= link_to "Show", person %><br>
<% end %> -->
